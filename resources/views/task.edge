@layout('main')
@section('container')

<h1>{{ title }}</h1>

<ul class="list-group">
    @each(item in lista)
        <li class="list-group-item">{{ item.title }}</li>
    @endeach
</ul>
  
@endsection
